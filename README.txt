CSC505 Module 3 assignment draft
Assignment: https://csuglobal.instructure.com/courses/122516/assignments/2255427

These are working drafts for review, not completed submission ZIP files.

Run with Python 3.10 or newer: python shopping_list_prototype.py
Only the Python standard library is required for shopping_list_prototype.py.

Environment setup (uses uv, https://docs.astral.sh/uv/):
  uv venv .venv
  uv pip install --python .venv matplotlib
The venv is only needed to regenerate shopping_screens.png (see below).
execution_output.txt contains actual captured output from a successful run.
The program output has been checked; screenshots have not been created.
Open the script in your IDE, run it, and capture the successful execution required by Canvas.

PNG files are the reviewed diagram previews used in the Word draft.
The .uxf files are editable UMLet XML drafts. They have not been rendered in UMLet.
Open and check them in UMLet, adjust layout as needed, and export PNG or PDF from the required diagram tool before submitting.
The shopping_screens.png file is the digital paper prototype (all five screens
with numbered navigation arrows). It is generated automatically by
make_paper_prototype.py using Python + matplotlib in sketch (xkcd) mode.
To regenerate it: .venv\Scripts\python.exe make_paper_prototype.py

The course textbook bibliography was confirmed in the syllabus. Review the relevant textbook discussion and add or refine in-text citations from your reading before final submission.

After reviewing and completing the missing items, put the exact required deliverables in a ZIP for that Canvas assignment.
